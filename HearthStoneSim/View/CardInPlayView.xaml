<UserControl x:Class="HearthStoneSim.View.CardInPlayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModel="clr-namespace:HearthStoneSim.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="300">

    <!--<UserControl.DataContext>
        <viewModel:CardViewModel />
    </UserControl.DataContext>-->

    <Viewbox HorizontalAlignment="Left"  MaxHeight="500">
        <Canvas Width="84" Height="120">
            <Ellipse Canvas.Left="7" Canvas.Top="2" Width="70" Height="100" Stroke="Blue">
                <Ellipse.Fill>
                    <ImageBrush ImageSource="{Binding ImageSource}" Stretch="UniformToFill"/>
                </Ellipse.Fill>
            </Ellipse>
        </Canvas>
    </Viewbox>
</UserControl>
